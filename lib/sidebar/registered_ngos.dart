import 'package:flutter/material.dart';

class RegisteredNgos extends StatelessWidget {
  const RegisteredNgos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('NGOS WITH US'),
      ),
    );
  }
}
